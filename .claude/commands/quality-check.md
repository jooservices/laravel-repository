# quality-check

Validate work against the repository command map:

- `composer lint`
- `composer lint:all`
- `composer test`
- `composer test:coverage`
- `composer ci`

Use the matching canonical skills from `.github/skills/` when assessing risk, docs drift, or workflow impact.
