---
description: "Implement or review a package change while preserving architecture, docs, tests, CI, and AI policy alignment."
mode: ask
---

You are working in `jooservices/laravel-repository`.

Task:
${input:Describe the package change, bugfix, docs change, or repository-governance update.}

Requirements:
- Read `AGENTS.md` first.
- Load the matching canonical skills from `.github/skills/` before making non-trivial changes.
- Keep changes minimal and module-appropriate.
- Update tests, docs, and workflow references when behavior or contributor policy changes.
- Use only the documented composer command map.
- Include a short risk summary before finishing.
