# CI Triage Prompt

Work in `jooservices/laravel-repository`.

Requirements:

- Read `AGENTS.md` first.
- Load `ci-hooks-maintenance`, `security-hardening`, and `review-and-risk-assessment`.
- Make secret, workflow-setting, and external-service assumptions explicit.
