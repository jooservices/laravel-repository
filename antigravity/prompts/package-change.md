# Package Change Prompt

Work in `jooservices/laravel-repository`.

Requirements:

- Read `AGENTS.md` first.
- Load the relevant `.github/skills/` files.
- Keep changes minimal and module-appropriate.
- Update docs and tests with the implementation.
- Use the canonical composer command map only.
