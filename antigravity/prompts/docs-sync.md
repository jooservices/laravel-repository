# Docs Sync Prompt

Work in `jooservices/laravel-repository`.

Requirements:

- Read `AGENTS.md` first.
- Load `documentation-sync` and related architecture or risk skills.
- Keep docs, examples, AI guidance, and command references aligned.
