# Claude Code Guide

Start with `AGENTS.md` for the repository-wide policy.

For non-trivial work, load the matching canonical skills from `.github/skills/` first. Use the adapter commands in `.claude/commands/` as thin entry points that mirror the same repository truth.
